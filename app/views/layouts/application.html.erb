<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Welcome </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
           
           <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "My Documents", my_document_path(current_user), :class=>"nav-link"  %>
            </li> 
            <li class="nav-item">
              <%= link_to "Other Documents", documents_path, :class=>"nav-link"  %>
            </li> 
            <li class="nav-item">
              <%= link_to "Account Setting", edit_user_path(current_user), :class=>"nav-link"  %>
            </li>  
            <li class="nav-item">
              <%= link_to "Log Out", destroy_user_session_path, method: :delete, :class=>"nav-link" %>
            </li>  
               <% if is_system_admin? %>
               <li class="nav-item">
                  <%= link_to "Manage User", users_path, :class=>"nav-link"  %>
                </li>  
              <% end %>  
            <% end %>             
        </ul>
      </div>
    </div>
  </nav>

  <body>
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
